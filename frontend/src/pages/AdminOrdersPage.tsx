import { useCallback,useEffect,useState } from 'react';import{Link}from'react-router-dom';import{StatusBadge}from'../components/commerce/StatusBadge';import{EmptyState}from'../components/feedback/EmptyState';import{ErrorState}from'../components/feedback/ErrorState';import{LoadingState}from'../components/feedback/LoadingState';import{adminCommerceService}from'../services/adminCommerceService';import type{AdminOrderPage}from'../types/admin';import{normalizeApiError}from'../utils/apiError';import{formatCurrency,formatDateTime}from'../utils/format';
export function AdminOrdersPage(){const[page,setPage]=useState(0);const[data,setData]=useState<AdminOrderPage|null>(null);const[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{try{setData(await adminCommerceService.listOrders(page));setError(null)}catch(e){setError(normalizeApiError(e).message)}},[page]);useEffect(()=>{void load()},[load]);if(!data&&!error)return <LoadingState label="Loading orders" cards/>;return <section className="commerce-page"><div className="page-heading"><p className="eyebrow">Admin commerce</p><h1>Orders</h1><p>Process standard customer orders and manage internal shipment tracking.</p></div>{error?<ErrorState title="We couldn't load orders" message={error} onRetry={()=>void load()}/>:!data?.content.length?<EmptyState title="No orders" message="Standard orders will appear here."/>:<><div className="order-list">{data.content.map(o=><article className="order-card" key={o.orderId}><div><p className="order-card-label">Order</p><h2>#{o.orderId}</h2><p>{o.customerEmail}</p></div><div><p className="order-card-label">Placed</p><time>{formatDateTime(o.createdAt)}</time></div><StatusBadge kind="order" value={o.status}/><strong>{formatCurrency(o.totalAmount)}</strong><Link className="button button-secondary" to={`/admin/orders/${o.orderId}`}>Manage</Link></article>)}</div>{data.total_pages>1&&<nav className="pagination"><button disabled={page===0} onClick={()=>setPage(page-1)}>Previous</button><span>Page {page+1} of {data.total_pages}</span><button disabled={page+1>=data.total_pages} onClick={()=>setPage(page+1)}>Next</button></nav>}</>}</section>}
