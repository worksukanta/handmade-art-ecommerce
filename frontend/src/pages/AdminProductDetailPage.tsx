import { useCallback, useEffect, useState } from 'react'
import type { FormEvent } from 'react'
import { Link, useLocation, useParams } from 'react-router-dom'
import { ErrorState } from '../components/feedback/ErrorState'
import { LoadingState } from '../components/feedback/LoadingState'
import { StatusBadge } from '../components/commerce/StatusBadge'
import { ProductImage } from '../components/catalogue/ProductImage'
import { adminCommerceService } from '../services/adminCommerceService'
import type { AdminProductDetail, AdminProductSummary } from '../types/admin'
import { normalizeApiError } from '../utils/apiError'
import { formatCurrency, humanizeStatus } from '../utils/format'

export function AdminProductDetailPage() {
  const id = Number(useParams().id); const location = useLocation(); const created = Boolean((location.state as { productCreated?: boolean } | null)?.productCreated); const [product,setProduct]=useState<AdminProductDetail|null>(null); const [choices,setChoices]=useState<AdminProductSummary[]>([]); const [error,setError]=useState<string|null>(null); const [busy,setBusy]=useState(false); const [message,setMessage]=useState<string|null>(created ? 'Product created successfully. You can now upload product images.' : null); const [file,setFile]=useState<File|null>(null); const [relatedId,setRelatedId]=useState('')
  const load=useCallback(async()=>{setError(null);try{const [detail,page]=await Promise.all([adminCommerceService.getProduct(id),adminCommerceService.listProducts(0,100)]);setProduct(detail);setChoices(page.content.filter(p=>p.id!==id))}catch(e){setError(normalizeApiError(e).message)}},[id]); useEffect(()=>{void load()},[load])
  const mutate=async(action:()=>Promise<unknown>,success:string)=>{setBusy(true);setError(null);setMessage(null);try{await action();setMessage(success);await load()}catch(e){const n=normalizeApiError(e);setError(n.status===409?`The product changed on the server. ${n.message}`:n.message);if(n.status===409)await load()}finally{setBusy(false)}}
  if(error&&!product)return <ErrorState title="We couldn't load this product" message={error} onRetry={()=>void load()}/>; if(!product)return <LoadingState label="Loading product"/>
  const relatedIds=product.related_products.map(p=>p.id)
  const upload=(e:FormEvent)=>{e.preventDefault();if(file)void mutate(()=>adminCommerceService.uploadProductImage(id,file),'Image uploaded.').then(()=>setFile(null))}
  const addRelated=()=>{const value=Number(relatedId);if(value&&!relatedIds.includes(value))void mutate(()=>adminCommerceService.replaceRelatedProducts(id,[...relatedIds,value]),'Related products updated.').then(()=>setRelatedId(''))}
  return <section className="commerce-page"><Link className="back-link" to="/admin/products">← Products</Link><div className="heading-actions"><div className="page-heading"><p className="eyebrow">Product #{id}</p><h1>{product.name}</h1><StatusBadge kind="generic" value={product.status}/></div><Link className="button" to={`/admin/products/${id}/edit`}>Edit product</Link></div>{error&&<p className="form-alert form-alert-error" role="alert">{error}</p>}{message&&<p className="form-alert form-alert-success" role="status">{message}</p>}<div className="admin-detail-grid"><div className="detail-panel"><h2>Catalogue details</h2><dl className="request-details"><div><dt>Type</dt><dd>{humanizeStatus(product.product_type)}</dd></div><div><dt>Category</dt><dd>{product.category_name}</dd></div><div><dt>Price</dt><dd>{formatCurrency(product.price)}</dd></div><div><dt>Stock</dt><dd>{product.availability.quantity_on_hand ?? 'Not tracked'}</dd></div><div><dt>Description</dt><dd>{product.description||'—'}</dd></div></dl><button className="button button-secondary" disabled={busy} onClick={()=>void mutate(()=>adminCommerceService.setProductStatus(id,product.status==='ACTIVE'?'INACTIVE':'ACTIVE'),`Product ${product.status==='ACTIVE'?'deactivated':'activated'}.`)}>{product.status==='ACTIVE'?'Deactivate':'Activate'}</button></div><div className="detail-panel"><h2>Images</h2><div className="admin-image-grid">{product.images.map(image=><article key={image.id}><ProductImage image={image} className="admin-product-image"/><p>{image.original_filename}</p><small>{image.is_primary?'Primary · ':''}Order {image.display_order}</small><button className="text-button danger" disabled={busy} onClick={()=>{if(confirm('Delete this image?'))void mutate(()=>adminCommerceService.deleteProductImage(id,image.id),'Image deleted.')}}>Delete</button></article>)}</div><form className="upload-form" onSubmit={upload}><label htmlFor="product-image">Upload image</label><input id="product-image" accept="image/*" type="file" onChange={e=>setFile(e.target.files?.[0]??null)}/><button className="button" disabled={!file||busy}>Upload</button></form></div><div className="detail-panel"><h2>Related products</h2>{product.related_products.length?<ul className="simple-list">{product.related_products.map(p=><li key={p.id}><span>{p.name} ({p.status})</span><button className="text-button danger" disabled={busy} onClick={()=>void mutate(()=>adminCommerceService.replaceRelatedProducts(id,relatedIds.filter(x=>x!==p.id)),'Related product removed.')}>Remove</button></li>)}</ul>:<p>No related products.</p>}<div className="inline-controls"><select aria-label="Product to relate" value={relatedId} onChange={e=>setRelatedId(e.target.value)}><option value="">Select product</option>{choices.filter(p=>!relatedIds.includes(p.id)).map(p=><option key={p.id} value={p.id}>{p.name} ({p.status})</option>)}</select><button className="button button-secondary" disabled={!relatedId||busy} onClick={addRelated}>Add</button></div></div></div></section>
}
